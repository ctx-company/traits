# @ctx-traits/agents

Shared `ctx.traits` CDK doctrine for reviewed multi-agent procedures, and the
vendorable canonical `agents` trait package built from that same doctrine.

## Install

```sh
pnpm add -D @ctx-traits/agents
```

## Source

https://github.com/ctx-company/traits (`packages/agents`).
