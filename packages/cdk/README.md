# @ctx-traits/cdk

Pure TypeScript builders for authoring deterministic `ctx.traits` specifications.
CDK does not execute the specs, internal `ctx.traits` runtime worker does.

## Install

```sh
pnpm add -D @ctx-traits/cdk
```

## Source

https://github.com/ctx-company/traits (`packages/cdk`).
