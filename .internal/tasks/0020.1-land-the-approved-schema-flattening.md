# 0020.1 — Land the approved schema-flattening diff

**Status:** ready to implement · **Depends on:** nothing · **Raised:** 2026-08-04 (split from 0020
after run-7b9d29ff exhausted max-iterations; the only 0020 child, because the work is DONE — this
task lands it, nothing more)

Run-7b9d29ff implemented all of 0020 and its reviewer **approved with zero blockers** on the first
round; the working tree then stayed byte-identical through eleven further review rounds while the
attached `just test` gate kept failing on a phantom — the leased `build-slots/slot-1` binary
reporting `packages/config/src/generated.ts` stale even though the diff touches neither
`harness_config.rs` nor any generated file and this worktree's own source build of
`sdk-generate --check` exits 0. That environmental red is 0099's failure class, and it is the whole
reason the run could not exit its loop.

The work is `.internal/tasks/assets/0020-flatten-core.patch` — 4 files, 446 insertions, exported
verbatim from the run's worktree (`wt-415567ef26c4`); `git apply --check` passed against main as of
2026-08-04. It contains:

- `modules/core/src/trait/schema.rs` (+224): `reject_recursive_schemas` DFS with path-bearing
  `manifest::Error::InvalidField` errors, plus 4 unit tests (`self_cycle_is_rejected`,
  `mutual_cycle_is_rejected`, `cycle_through_list_wrapper_is_rejected`,
  `deep_acyclic_chain_flattens_without_truncation`);
- `modules/cli/src/app/frame_prompt.rs` (+26/−3): `MAX_SCHEMA_RESOLUTION_STEPS=64` backstop that
  `debug_assert`s and warns, replacing the `MAX_SCHEMA_REF_DEPTH=4` silent truncation that could
  quietly emit `{}`; counts list-wrapper hops via depth+1;
- `modules/cli/tests/proof_schema_flatten.rs` (new): the two contract proofs —
  `shared_nested_schema_is_fully_inlined_with_no_ref_or_defs` and
  `recursive_schema_graph_fails_at_build_with_a_named_cycle_path`;
- `packages/cdk/test/cdk.test.ts` (+1): `$defs` rejection case.

Both consumers (`<schema>` block and `--json-schema` payload) provably read the single resolved
Value from `requested_output_schema` (verified by the reviewer at `frame_prompt.rs:1244-1298`), so
one flattening path serves both — the parent task's core contract.

## Scope

Apply the patch as one commit whose subject credits run-7b9d29ff's work, gate, land. Reviewer-carried
minor notes that are explicitly NOT in scope (recorded so nobody relitigates them into this run):
union-arm bracket-stripping in `referenced_local_ids` stays manual until union emit lands, and a
dedicated union-cycle unit test rides with that same future work.

## Watch

- **Do not run the gate against a stale leased slot.** The run's twelve gate attempts all failed on
  the slot-1 binary predating the current sources. Force a rebuild of the gate binary from this
  worktree's sources first (isolate `CARGO_TARGET_DIR` if the shared slot misbehaves); if the
  "stale generated SDK file" complaint appears while a source-built `sdk-generate --check` exits 0,
  that is 0099's class — fix the binary, never the generated file.
- Expected green evidence, as the reviewer last ran it: `cargo test -p ctx-traits-core schema`
  (6 passed) and `cargo test -p ctx-traits-cli --test proof_schema_flatten` (2 passed).

## Done when

The patch content is one commit on the run branch; the repository's unmodified `just test` exits 0
with an sdk-check binary built from current sources; the schema unit tests and both
`proof_schema_flatten` proofs pass; and the work lands on `main` through the normal merge path.
