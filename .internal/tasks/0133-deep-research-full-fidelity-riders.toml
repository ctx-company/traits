schema-version = "0.2"
key = "0133"
title = "deep-research full-fidelity riders"
status = "ready"
raised = "2026-08-05"
content = """


The riders deferred from the deep-research migration, to be dispatched only after
the live validation (0132) harvests real gaps:

- Wave parallelism over research streams (the concurrency machinery landed with
  Group 66/P344's line; verify the current concurrent for-each state before
  building on it).
- Numeric quality-score guards (the comparators landed; wire them where the
  validation shows the serial gate is insufficient).
- Worktree-sandboxed research outputs.
- Mid-run gate-override UX as its own runtime note.
- Plus: run `ctx traits import` on the original source AGENTS.md and record the
  scaffold diff as case-study material for the adoption funnel.

"""
validation = """
## Done when

Each rider is either implemented against a gap the 0132 validation actually
recorded, or explicitly dropped with the validation's evidence cited — no rider
built speculatively.

Full original contract: `archived/board/execution-plan.md` (Group 97, P400).
"""
