schema-version = "0.2"
key = "0116"
title = "Auto-research writeup + story integration: the pre-registered experiment case study"
status = "ready"
raised = "2026-08-05"
content = """


The auto-research trait and its self-improvement showcase landed (P385/P386): the
experiment loop's keep/discard decision is DECLARED — a guard condition in a
validated, digested artifact over a TYPED command output — where competitors' demos
trust the program author. The writeup is the missing launch-content half.

Run the showcase on one real trait (e.g. optimize a distill prompt), publish the run
as the case study: the protocol artifact (pre-registered — reviewable before a
single GPU-hour is spent), per-experiment story view, kept-chain digests (every kept
mutation a digested diff, the whole optimization a byte-stable ledger).

"""
validation = """
## Watch

- Claim-gate everything.
- `story` rendering helps presentation but plain ledger dumps suffice for v1.
- The framing from the plan: "self-improving agents with provenance" — the artifact
  under optimization is itself a validated artifact.

## Done when

A real self-improvement run is published as the case study with protocol artifact,
per-experiment narrative, and kept-chain digests, every claim gated.

Full original contract: `archived/board/execution-plan.md` (Group 98, P385–P387).
"""
