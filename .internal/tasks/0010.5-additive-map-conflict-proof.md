# 0010.5 — Prove additive-map conflicts behave, in the resolver and on the public path

**Status:** ready to implement · **Raised:** 2026-08-03 (split out of 0010 after run-a3e5fc88 parked at max-iterations with five open blockers; 0010's design decisions are LANDED — these are the implementation remainder)

Carried from blocker `additive-map-conflicts-lack-diagnostic` (2/3 steps done — implementation
exists, the behavioural proof does not).

## What already landed

Per-entry authored value and source metadata is captured for `worktree.env.*` and
`run.build-cache.*`, retaining the effective repository source per key, and one structured conflict
is generated for each differing personal or `$CTX_CONFIG` declaration rejected by a
repository-owned additive-map key.

## Remaining

- Resolver and public text/JSON tests covering distinct-key merges (both sides contribute, nothing
  is lost) and same-key repository conflicts from BOTH personal sources (`~/.config` and
  `$CTX_CONFIG`).

## Done when

`cargo test -p ctx-traits-io harness_config` and `cargo test -p ctx-traits-cli --test proof_config`
pass with tests proving repository-owned `worktree.env` and `run.build-cache` keys survive
conflicting personal and `$CTX_CONFIG` declarations, and that distinct keys from different tiers
merge rather than replace.
