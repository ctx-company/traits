ctx traits preview
  trait: implement
  session: none
--- step capture-the-session-base [Capture the session base] transport=command ---
  note: command step: dispatched via the runtime IO edge, not an agent — no agent prompt is composed for this frame
Step [run 0 / source 0]: Capture the session base
Command step: runtime-only; static hosts cannot execute it.
Command argv: git rev-parse HEAD
Output slot: slot:diff-base
Status: blocked-command-permission-required unless the adapter has explicit approval.
Available inputs:
Requested outputs:
- slot:diff-base (replace)

--- step draft-the-implementation-plan [Draft the implementation plan] transport=static ---
  note: no session: only accepted values are inlined; pending inputs are declared inline in the resolved prompt below
<information>
  <identity>You are agent:smart. Do only this step's work.</identity>
  <title>Draft the implementation plan</title>
  <description>Reviewed implementation: implement, review, refine & commit.</description>
  <prompt>

        Draft the implementation plan for {port:task}, read it and the relevant code with your tools.
        Scope, files to touch, approach, reuse opportunities, validation plan, risks.
  </prompt>
<intent group="require">Conditions the finished work has to meet.</intent>
<intent group="require" id="correctness">
  Whether the code does the right thing, ahead of how it reads.
</intent>
<intent group="require" id="robustness">
  Correctness under ordinary failure and boundary conditions.
</intent>
<intent group="require" id="leanness">
  Only what the requested outcome needs.
</intent>
<intent group="require" id="reuse-over-reimplement">
  Extracting shared logic instead of copying it.
</intent>
<intent group="require" id="review-before-final">
  Inspecting completed work against its requirements before calling it done.
</intent>
<intent group="require" id="bounded-refinement">
  A review-and-repair loop with a condition that ends it.
</intent>
<intent group="avoid">Not acceptable, unless unavoidable.</intent>
<intent group="avoid" id="over-engineering">
  Generality built ahead of demonstrated need.
</intent>
<intent group="avoid" id="duplication">
  Logic copied where it should be shared and evolve together.
</intent>
<intent group="avoid" id="scope-creep">
  Expanding past what was asked.
</intent>
<intent group="avoid" id="unbounded-loop">
  Refinement with no stop condition.
</intent>
<intent group="avoid" id="rubber-stamp-review">
  Approval without substantive review.
</intent>
</information>

<input>
  <data>
    <task pending="external input, not yet accepted" />
  </data>
</input>

<output>
  <format>
    {
      "draft": "string"
    }
  </format>

  <budget>Your entire response must fit in 294912 bytes.</budget>
  <response>
    Return ONLY one JSON object matching <format> — no prose before or after it, no code fences, no extra top-level fields. String-typed fields are single strings, never arrays.
  </response>
</output>

--- step implement-the-task [Implement the task] transport=static ---
  note: no session: only accepted values are inlined; pending inputs are declared inline in the resolved prompt below
<information>
  <identity>You are agent:worker. Do only this step's work.</identity>
  <title>Implement the task</title>
  <description>Reviewed implementation: implement, review, refine & commit.</description>
  <prompt>

        Implement {port:task} following the draft {slot:draft} — the task's file lives in .internal/tasks/ (matched by key, name, or filename).
        Honor Watch, and treat Done when as the definition of done: run the checks it names before reporting.
        Optionally attached verdicts from previous rounds: {slot:review-verdict-1} & {slot:review-verdict-2}
        Optionally attached work summary from previous rounds: {slot:work-summary}
        Attached verdicts are the source of truth — always follow their guidance.



        Return the cumulative work summary (what changed, how it was validated & open concerns): slot:work-summary

  </prompt>
<intent group="require">Conditions the finished work has to meet.</intent>
<intent group="require" id="correctness">
  Whether the code does the right thing, ahead of how it reads.
</intent>
<intent group="require" id="robustness">
  Correctness under ordinary failure and boundary conditions.
</intent>
<intent group="require" id="leanness">
  Only what the requested outcome needs.
</intent>
<intent group="require" id="reuse-over-reimplement">
  Extracting shared logic instead of copying it.
</intent>
<intent group="require" id="review-before-final">
  Inspecting completed work against its requirements before calling it done.
</intent>
<intent group="require" id="bounded-refinement">
  A review-and-repair loop with a condition that ends it.
</intent>
<intent group="avoid">Not acceptable, unless unavoidable.</intent>
<intent group="avoid" id="over-engineering">
  Generality built ahead of demonstrated need.
</intent>
<intent group="avoid" id="duplication">
  Logic copied where it should be shared and evolve together.
</intent>
<intent group="avoid" id="scope-creep">
  Expanding past what was asked.
</intent>
<intent group="avoid" id="unbounded-loop">
  Refinement with no stop condition.
</intent>
<intent group="avoid" id="rubber-stamp-review">
  Approval without substantive review.
</intent>
</information>

<input>
  <data>
    <task pending="external input, not yet accepted" />
    <draft pending="produced by step draft-the-implementation-plan" />
  </data>
</input>

<output>
  <format>
    {
      "work-summary": "string"
    }
  </format>

  <budget>Your entire response must fit in 294912 bytes.</budget>
  <response>
    Return ONLY one JSON object matching <format> — no prose before or after it, no code fences, no extra top-level fields. String-typed fields are single strings, never arrays.
  </response>
</output>

--- step capture-the-changed-files [Capture the changed files] transport=command ---
  note: command step: dispatched via the runtime IO edge, not an agent — no agent prompt is composed for this frame
Step [run 2 / source 1]: Capture the changed files
Command step: runtime-only; static hosts cannot execute it.
Command argv: git diff --name-status {slot:diff-base}
Output slot: slot:changed-files
Status: blocked-command-permission-required unless the adapter has explicit approval.
Available inputs:
slot:diff-base: (absent — no accepted value yet)
Requested outputs:
- slot:changed-files (replace)

--- step review-the-implementation [Review the implementation] transport=static ---
  note: no session: only accepted values are inlined; pending inputs are declared inline in the resolved prompt below
<information>
  <identity>You are agent:smart. Do only this step's work.</identity>
  <title>Review the implementation</title>
  <description>Reviewed implementation: implement, review, refine & commit.</description>
  <prompt>

        Review the working tree's implemented state for {port:task} against the task file's own contract as source of truth.
        A BLOCKER is a correctness bug, an unmet item of the task's own Done when, clear over-build, or un-abstracted duplication.
        Say "not yet" as many rounds as it takes; do not approve to end the loop, and do not invent a blocker to extend it.
        Optionally attached verdicts from previous rounds: {slot:review-verdict-1} & {slot:review-verdict-2}
        Worker's summary: {slot:work-summary}, Changed files {slot:changed-files}.



        Return the typed review verdict — status is revise while any blocker remains, approved when none do (slot:review-verdict-1)

  </prompt>
<intent group="require">Conditions the finished work has to meet.</intent>
<intent group="require" id="correctness">
  Whether the code does the right thing, ahead of how it reads.
</intent>
<intent group="require" id="robustness">
  Correctness under ordinary failure and boundary conditions.
</intent>
<intent group="require" id="leanness">
  Only what the requested outcome needs.
</intent>
<intent group="require" id="reuse-over-reimplement">
  Extracting shared logic instead of copying it.
</intent>
<intent group="require" id="review-before-final">
  Inspecting completed work against its requirements before calling it done.
</intent>
<intent group="require" id="bounded-refinement">
  A review-and-repair loop with a condition that ends it.
</intent>
<intent group="avoid">Not acceptable, unless unavoidable.</intent>
<intent group="avoid" id="over-engineering">
  Generality built ahead of demonstrated need.
</intent>
<intent group="avoid" id="duplication">
  Logic copied where it should be shared and evolve together.
</intent>
<intent group="avoid" id="scope-creep">
  Expanding past what was asked.
</intent>
<intent group="avoid" id="unbounded-loop">
  Refinement with no stop condition.
</intent>
<intent group="avoid" id="rubber-stamp-review">
  Approval without substantive review.
</intent>
</information>

<input>
  <data>
    <task pending="external input, not yet accepted" />
    <work-summary pending="produced by step implement-the-task" />
    <changed-files pending="produced by step capture-the-changed-files" />
  </data>
</input>

<output>
  <format>
    {
      "review-verdict-1": "review-verdict-1"
    }
  </format>

  <schema>
    <review-verdict-1>{"additionalProperties":false,"description":"Typed review verdict. The loop blocks on genuine defects, clear over-build, and un-abstracted duplication — not on taste; status is revise if and only if a blocking defect remains; escalation flags run-level owner blockers for triage without stopping the loop; owner-items records the verified outside-capability remainder so the run can complete honestly.","properties":{"advisory":{"description":"Non-blocking notes: subjective style, naming, taste, optional improvements, follow-up work. Never affects status and never forces another refinement round.","type":"string"},"blockers":{"description":"The blocking defects that must be fixed before merge: correctness bugs, failing validation gates, clear over-build (accretion, defensive validation for states that cannot occur, scope creep beyond the task), OR un-abstracted duplication — logic that duplicates or closely resembles code elsewhere and should be unified into a shared abstraction instead of re-implemented or copied beside. Each entry carries a stable id, root cause, the required-fix invariant, and a falsifiable done-when. Non-empty when status is revise; an empty list (never omitted — always return the key) when approved. Always present so the runtime can deterministically copy it into a park report without a missing-field failure.","items":{"additionalProperties":false,"description":"One blocking defect: stable identity, location, root cause, the invariant an acceptable fix must establish, and the falsifiable check that clears it.","properties":{"done-when":{"description":"A falsifiable check the reviewer will apply next round to declare this blocker fixed. Wherever the check can be a COMMAND, state it as one the worker can run itself (a grep proving a function has exactly one caller, a test invocation) — the worker iterates against build-and-test signals, and a structural requirement stated only as prose is invisible to that loop: the worker stops where its own instruments read green, honestly believing the blocker addressed.","type":"string"},"id":{"description":"Stable kebab-case slug for this defect, chosen on first report and reused verbatim in every later round it survives (e.g. unrelated-change-guard-baseline).","type":"string"},"recurrence-of":{"description":"id of the prior-round blocker this restates; present when the prior fix did not clear its done-when.","type":"string"},"required-fix":{"description":"One or two sentences of intent: the invariant an acceptable fix establishes and why. The operational content — what to do, in what order — lives in `steps`, which is the field the worker executes and the field you maintain across rounds.","type":"string"},"root-cause":{"description":"Why the code is wrong — the missing or broken invariant, not just where it shows — and how deep the wrong goes: a slip at the cited line, a missing invariant, or a structure the code must converge to. The depth tells the worker how big a change you expect; omitting it is how a structural demand gets answered with a point-patch.","type":"string"},"rule-quote":{"description":"The exact authoritative line(s) the cited rule quotes, verified against the rule-authority resource. Present only when the blocker cites a standing product rule.","type":"string"},"rule-source":{"description":"Repo-relative path + section for the house rule this blocker cites, verified directly against the rule-authority resource. Present only when the blocker cites a standing product rule.","type":"string"},"steps":{"description":"The fix as an ordered list of typed steps — you are the smarter model here, and this list exists to spend that intelligence on the worker's behalf (owner ruling 2026-07-30). Each step is one operation: what to create and what it owns, what must cease to exist or shrink to a delegate, which call sites to route. A destination (\"replace the separate paths with one renderer\") is not a step; the operations that reach it are. THIS LIST IS CUMULATIVE STATE, not a fresh derivation: when this blocker appeared in your prior verdict (attached), carry its steps forward VERBATIM — same order, same text — flipping status to done only where you verified the evidence with your own tools, and appending genuinely new findings as new steps at the end. Never renumber, reword, drop, or re-derive a carried step: a vanished step erases the worker's credit, a reworded one moves the target, and both teach the worker that completing steps is pointless — the four-day stall this field exists to end.","items":{"additionalProperties":false,"description":"One step of a blocker's required fix, with verified progress state.","properties":{"evidence":{"description":"Why status is done: file:line, a test name, or a command and its observed result. Required in practice for every done step; absent while open.","type":"string"},"status":{"description":"\"open\" or \"done\". Flips to done only when the reviewer has verified the evidence against the working tree — never on the worker's claim alone.","type":"string"},"step":{"description":"The operation, frozen once written: imperative, concrete, one action. Carried verbatim across rounds.","type":"string"}},"required":["status","step"],"type":"object"},"type":"array"},"what":{"description":"The defect and the concrete failure it causes.","type":"string"},"where":{"description":"Repo-relative files and paths involved.","type":"string"}},"required":["done-when","id","required-fix","root-cause","steps","what","where"],"type":"object"},"type":"array"},"escalation":{"description":"needs-owner if and only if at least one blocker cannot be cleared by the worker from inside this run: the task file is a placeholder or lacks a falsifiable Done-when; a prerequisite task it names has not landed in this tree; the task is marked superseded or cancelled; resolving it requires an owner/authority decision; or a required gate fails for reasons outside the task's scope (tooling conflict). Escalation is RECORDED for owner triage — it does not stop the loop; refinement continues on every fixable blocker. none otherwise.","enum":["none","needs-owner"],"type":"string"},"escalation-reason":{"description":"One plain sentence: WHY the flagged blocker set is outside this run's authority and what owner action would clear it. Non-empty when escalation is needs-owner; an empty string (never omitted — always return the key) otherwise. Always present so the runtime can deterministically copy it into a park report without a missing-field failure.","type":"string"},"owner-items":{"description":"Checklist items of THIS task that no in-run effort can satisfy, accepted per the draft's SCOPE SPLIT (or a verified mid-run wall claim), each with its reason class, the substitute evidence you verified, and the owner close-out. Approving with this list certifies every agent-doable item is 100% implemented. Never doable work — promote any owner-only claim a shell could satisfy to a blocker; an entry with missing or unverified substitute evidence belongs in blockers, not here. Absent when every item is agent-doable.","items":{"additionalProperties":false,"description":"A checklist item outside in-run capability or authority, with verified substitute evidence and the owner action that closes it.","properties":{"class":{"description":"Why no in-run effort can satisfy it: needs a real screen; needs money or an owner-authorized live execution; needs an owner authority call; or contradicts landed code/authoritative rule and the resolution is the owner's.","enum":["gui-or-visual","paid-or-live-execution","owner-decision","contract-conflict"],"type":"string"},"close-out":{"description":"The exact command the owner runs or decision the owner makes to finish the item.","type":"string"},"item":{"description":"The checklist item or Done-when clause, quoted or tightly paraphrased.","type":"string"},"reason":{"description":"One sentence: why this item is outside in-run capability or authority.","type":"string"},"substitute-evidence":{"description":"The closest in-run verification actually produced and verified by the reviewer (tests, dry runs, static checks); \"none possible\" only when truly nothing applies.","type":"string"}},"required":["class","close-out","item","reason","substitute-evidence"],"type":"object"},"type":"array"},"remaining":{"description":"Cross-task seam citations ONLY: counterpart work belonging to a DIFFERENT task on the task board, each citing that task's file name. Never in-task scope — an unimplemented item of THIS task's checklist is a blocker, and unimplemented in-task scope recorded here instead of in blockers falsifies the verdict. Absent when no cross-task seam exists.","type":"string"},"status":{"description":"approved when no blocking defect remains (advisory notes may still exist); revise only when at least one blocking defect remains. Set to approved if and only if blockers is empty.","enum":["approved","revise"],"type":"string"},"wall-id":{"description":"Stable wall id copied VERBATIM from an explicit \"**Wall:** <id>\" label in the task file, non-empty only when status is revise and that label exists — never inferred from prose similarity or blocker content. Enables cross-run standing-wall refusal (P414); an empty string here never blocks a sibling run no matter how related its blockers look. Always present (never omitted) so the runtime can deterministically copy it into a park report without a missing-field failure.","type":"string"}},"required":["blockers","escalation","escalation-reason","status","wall-id"],"type":"object"}</review-verdict-1>
  </schema>

  <budget>Your entire response must fit in 294912 bytes.</budget>
  <response>
    Return ONLY one JSON object matching <schema>; <format> is a shape summary only — no prose before or after it, no code fences, no extra top-level fields. String-typed fields are single strings, never arrays.
  </response>
</output>

--- step check-working-tree-status [Check working tree status] transport=command ---
  note: command step: dispatched via the runtime IO edge, not an agent — no agent prompt is composed for this frame
Step [run 3 / source 3]: Check working tree status
Command step: runtime-only; static hosts cannot execute it.
Command argv: git status --porcelain
Output slot: slot:git-status
Status: blocked-command-permission-required unless the adapter has explicit approval.
Available inputs:
Requested outputs:
- slot:git-status (replace)

--- step write-the-commit-message [Write the commit message] transport=static ---
  note: no session: only accepted values are inlined; pending inputs are declared inline in the resolved prompt below
<information>
  <identity>You are agent:scribe. Do only this step's work.</identity>
  <title>Write the commit message</title>
  <description>Reviewed implementation: implement, review, refine & commit.</description>
  <prompt>

        The work for {port:task} is being committed.
        Write a concise commit message from the work summary into {slot:work-summary}.



        Return exactly the finished commit message into (slot:commit-message).

  </prompt>
<intent group="require">Conditions the finished work has to meet.</intent>
<intent group="require" id="correctness">
  Whether the code does the right thing, ahead of how it reads.
</intent>
<intent group="require" id="robustness">
  Correctness under ordinary failure and boundary conditions.
</intent>
<intent group="require" id="leanness">
  Only what the requested outcome needs.
</intent>
<intent group="require" id="reuse-over-reimplement">
  Extracting shared logic instead of copying it.
</intent>
<intent group="require" id="review-before-final">
  Inspecting completed work against its requirements before calling it done.
</intent>
<intent group="require" id="bounded-refinement">
  A review-and-repair loop with a condition that ends it.
</intent>
<intent group="avoid">Not acceptable, unless unavoidable.</intent>
<intent group="avoid" id="over-engineering">
  Generality built ahead of demonstrated need.
</intent>
<intent group="avoid" id="duplication">
  Logic copied where it should be shared and evolve together.
</intent>
<intent group="avoid" id="scope-creep">
  Expanding past what was asked.
</intent>
<intent group="avoid" id="unbounded-loop">
  Refinement with no stop condition.
</intent>
<intent group="avoid" id="rubber-stamp-review">
  Approval without substantive review.
</intent>
</information>

<input>
  <data>
    <task pending="external input, not yet accepted" />
    <work-summary pending="produced by step implement-the-task" />
  </data>
</input>

<output>
  <format>
    {
      "commit-message": "string"
    }
  </format>

  <budget>Your entire response must fit in 294912 bytes.</budget>
  <response>
    Return ONLY one JSON object matching <format> — no prose before or after it, no code fences, no extra top-level fields. String-typed fields are single strings, never arrays.
  </response>
</output>

--- step stage-all-changes [Stage all changes] transport=command ---
  note: command step: dispatched via the runtime IO edge, not an agent — no agent prompt is composed for this frame
Step [run 4 / source 1]: Stage all changes
Command step: runtime-only; static hosts cannot execute it.
Command argv: git add -A
Output slot: slot:stage-output
Status: blocked-command-permission-required unless the adapter has explicit approval.
Available inputs:
Requested outputs:
- slot:stage-output (replace)

--- step commit-the-work [Commit the work] transport=command ---
  note: command step: dispatched via the runtime IO edge, not an agent — no agent prompt is composed for this frame
Step [run 4 / source 2]: Commit the work
Command step: runtime-only; static hosts cannot execute it.
Command argv: git commit -m {slot:commit-message}
Output slot: port:commit-report
Status: blocked-command-permission-required unless the adapter has explicit approval.
Available inputs:
slot:commit-message: (absent — no accepted value yet)
Requested outputs:
- port:commit-report (replace)

