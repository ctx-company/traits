# Definition of done

The change is done only when every item is true:

- A test covers the change and fails before it, passes after it.
- The project's gates were run and are green on the working tree.
- The diff is limited to the request; no unrelated edits.
- The commit message states what changed and how it was validated.
