schema-version = "0.1"

# Package-shipped budget defaults for deep-research (0036: budget-only,
# package-tier document, superseding the old config.toml sidecar).

max-frames = 40
frame-seconds = 1800
total-seconds = 14400
max-retries = 2
idle-seconds = 900
