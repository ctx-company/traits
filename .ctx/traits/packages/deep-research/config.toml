schema-version = "0.1"

# Package-shipped budget defaults for deep-research (P312: budget-only
# sidecar). Mirrors .ctx/runs/profiles/deep-research.toml [budget], dead
# text since P312 made the sidecar the only budget tier below CLI flags
# (until P314).

[budget]
max-frames = 40
frame-seconds = 1800
total-seconds = 14400
max-retries = 2
idle-seconds = 900
