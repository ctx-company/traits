id = "review"
schema-version = "0.3"
version = "0.1.0"
name = "Review (Default)"
summary = "Points a single reviewer at an arbitrary locally-resolvable git ref/range and returns a typed verdict plus a rendered review document, with zero mutation of the reviewed tree."
variant = "default"

[metadata]
tag = [
    "first-party",
    "review",
    "read-only",
]

[[intent.require]]
id = "review-before-final"

[[intent.require]]
id = "leanness"

[[intent.avoid]]
id = "rubber-stamp-review"

[[intent.avoid]]
id = "scope-creep"

[[port]]
id = "range"
direction = "input"
schema = "schema:text"
description = 'A locally-resolvable git ref or range (e.g. "main...feature-x", a branch name, or a single ref meaning <merge-base of default>..ref). Preflight resolves and validates it with git rev-parse/git merge-base before anything else runs.'

[[port]]
id = "review-document-report"
direction = "output"
schema = "schema:text"
optional = true
description = "The rendered human review document. Absent when preflight parked on an unresolvable range or an empty diff."
value = "slot:review-document"

[[port]]
id = "review-verdict-report"
direction = "output"
schema = "schema:review-verdict"
optional = true
description = "The reviewer's typed verdict. Absent when preflight parked on an unresolvable range or an empty diff."
value = "slot:review-verdict"

[[port]]
id = "tree-status-report"
direction = "output"
schema = "schema:review-tree-status"
optional = true
description = "Whether the reviewed tree came out clean. Absent when preflight parked on an unresolvable range or an empty diff."
value = "slot:tree-status"

[[slot]]
id = "commit-log"
schema = "schema:text"
description = "git log --oneline output for the resolved range."

[[slot]]
id = "diff-stat"
schema = "schema:text"
description = "git diff --stat output for the resolved range: file inventory only, never patch bodies."
hint = "Index pattern — the reviewer opens actual content with its own tools (git show, git diff <range> -- <file>)."

[[slot]]
id = "range-resolved"
schema = "schema:text"
description = "The normalized two-endpoint range actually diffed and logged, after preflight resolution."

[[slot]]
id = "review-document"
schema = "schema:text"
description = "Rendered human review document for the range, written by the same reviewer step."

[[slot]]
id = "review-verdict"
schema = "schema:review-verdict"
description = "Reviewer's typed verdict on the range. No task board governs this run: wall-id is the empty string, remaining/owner-items are absent, and escalation is needs-owner only for authority questions the diff itself raises."

[[slot]]
id = "tree-status"
schema = "schema:review-tree-status"
description = "Detects (does not prevent) whether the review step mutated the reviewed tree."

[[schema]]
id = "blocker"
description = "One blocking defect: stable identity, location, root cause, the invariant an acceptable fix must establish, and the falsifiable check that clears it."

[schema.fields.done-when]
schema = "schema:text"
required = true
description = "A falsifiable check the reviewer will apply next round to declare this blocker fixed. Wherever the check can be a COMMAND, state it as one the worker can run itself (a grep proving a function has exactly one caller, a test invocation) — the worker iterates against build-and-test signals, and a structural requirement stated only as prose is invisible to that loop: the worker stops where its own instruments read green, honestly believing the blocker addressed."

[schema.fields.id]
schema = "schema:text"
required = true
description = "Stable kebab-case slug for this defect, chosen on first report and reused verbatim in every later round it survives (e.g. unrelated-change-guard-baseline)."

[schema.fields.recurrence-of]
schema = "schema:text"
description = "id of the prior-round blocker this restates; present when the prior fix did not clear its done-when."

[schema.fields.required-fix]
schema = "schema:text"
required = true
description = "One or two sentences of intent: the invariant an acceptable fix establishes and why. The operational content — what to do, in what order — lives in `steps`, which is the field the worker executes and the field you maintain across rounds."

[schema.fields.root-cause]
schema = "schema:text"
required = true
description = "Why the code is wrong — the missing or broken invariant, not just where it shows — and how deep the wrong goes: a slip at the cited line, a missing invariant, or a structure the code must converge to. The depth tells the worker how big a change you expect; omitting it is how a structural demand gets answered with a point-patch."

[schema.fields.rule-quote]
schema = "schema:text"
description = "The exact authoritative line(s) the cited rule quotes, verified against the rule-authority resource. Present only when the blocker cites a standing product rule."

[schema.fields.rule-source]
schema = "schema:text"
description = "Repo-relative path + section for the house rule this blocker cites, verified directly against the rule-authority resource. Present only when the blocker cites a standing product rule."

[schema.fields.steps]
schema = "[schema:blocker-step]"
required = true
description = """The fix as an ordered list of typed steps — you are the smarter model here, and this list exists to spend that intelligence on the worker's behalf (owner ruling 2026-07-30). Each step is one operation: what to create and what it owns, what must cease to exist or shrink to a delegate, which call sites to route. A destination ("replace the separate paths with one renderer") is not a step; the operations that reach it are. THIS LIST IS CUMULATIVE STATE, not a fresh derivation: when this blocker appeared in your prior verdict (attached), carry its steps forward VERBATIM — same order, same text — flipping status to done only where you verified the evidence with your own tools, and appending genuinely new findings as new steps at the end. Never renumber, reword, drop, or re-derive a carried step: a vanished step erases the worker's credit, a reworded one moves the target, and both teach the worker that completing steps is pointless — the four-day stall this field exists to end."""

[schema.fields.what]
schema = "schema:text"
required = true
description = "The defect and the concrete failure it causes."

[schema.fields.where]
schema = "schema:text"
required = true
description = "Repo-relative files and paths involved."

[[schema]]
id = "blocker-step"
description = "One step of a blocker's required fix, with verified progress state."

[schema.fields.evidence]
schema = "schema:text"
description = "Why status is done: file:line, a test name, or a command and its observed result. Required in practice for every done step; absent while open."

[schema.fields.status]
schema = "schema:text"
required = true
description = """"open" or "done". Flips to done only when the reviewer has verified the evidence against the working tree — never on the worker's claim alone."""

[schema.fields.step]
schema = "schema:text"
required = true
description = "The operation, frozen once written: imperative, concrete, one action. Carried verbatim across rounds."

[[schema]]
id = "owner-item"
description = "A checklist item outside in-run capability or authority, with verified substitute evidence and the owner action that closes it."

[schema.fields.class]
schema = "schema:text"
required = true
description = "Why no in-run effort can satisfy it: needs a real screen; needs money or an owner-authorized live execution; needs an owner authority call; or contradicts landed code/authoritative rule and the resolution is the owner's."
allowed = [
    "gui-or-visual",
    "paid-or-live-execution",
    "owner-decision",
    "contract-conflict",
]

[schema.fields.close-out]
schema = "schema:text"
required = true
description = "The exact command the owner runs or decision the owner makes to finish the item."

[schema.fields.item]
schema = "schema:text"
required = true
description = "The checklist item or Done-when clause, quoted or tightly paraphrased."

[schema.fields.reason]
schema = "schema:text"
required = true
description = "One sentence: why this item is outside in-run capability or authority."

[schema.fields.substitute-evidence]
schema = "schema:text"
required = true
description = 'The closest in-run verification actually produced and verified by the reviewer (tests, dry runs, static checks); "none possible" only when truly nothing applies.'

[[schema]]
id = "review-tree-status"

[schema.fields.argv]
schema = "[schema:text]"
required = true
description = "The exact argv that decided it."

[schema.fields.ok]
schema = "schema:boolean"
required = true
description = "True when the reviewed tree has no uncommitted changes after the review step ran."

[[schema]]
id = "review-verdict"
description = "Typed review verdict. The loop blocks on genuine defects, clear over-build, and un-abstracted duplication — not on taste; status is revise if and only if a blocking defect remains; escalation flags run-level owner blockers for triage without stopping the loop; owner-items records the verified outside-capability remainder so the run can complete honestly."

[schema.fields.advisory]
schema = "schema:text"
description = "Non-blocking notes: subjective style, naming, taste, optional improvements, follow-up work. Never affects status and never forces another refinement round."

[schema.fields.blockers]
schema = "[schema:blocker]"
required = true
description = "The blocking defects that must be fixed before merge: correctness bugs, failing validation gates, clear over-build (accretion, defensive validation for states that cannot occur, scope creep beyond the task), OR un-abstracted duplication — logic that duplicates or closely resembles code elsewhere and should be unified into a shared abstraction instead of re-implemented or copied beside. Each entry carries a stable id, root cause, the required-fix invariant, and a falsifiable done-when. Non-empty when status is revise; an empty list (never omitted — always return the key) when approved. Always present so the runtime can deterministically copy it into a park report without a missing-field failure."

[schema.fields.escalation]
schema = "schema:text"
required = true
description = "needs-owner if and only if at least one blocker cannot be cleared by the worker from inside this run: the task file is a placeholder or lacks a falsifiable Done-when; a prerequisite task it names has not landed in this tree; the task is marked superseded or cancelled; resolving it requires an owner/authority decision; or a required gate fails for reasons outside the task's scope (tooling conflict). Escalation is RECORDED for owner triage — it does not stop the loop; refinement continues on every fixable blocker. none otherwise."
allowed = [
    "none",
    "needs-owner",
]

[schema.fields.escalation-reason]
schema = "schema:text"
required = true
description = "One plain sentence: WHY the flagged blocker set is outside this run's authority and what owner action would clear it. Non-empty when escalation is needs-owner; an empty string (never omitted — always return the key) otherwise. Always present so the runtime can deterministically copy it into a park report without a missing-field failure."

[schema.fields.owner-items]
schema = "[schema:owner-item]"
description = "Checklist items of THIS task that no in-run effort can satisfy, accepted per the draft's SCOPE SPLIT (or a verified mid-run wall claim), each with its reason class, the substitute evidence you verified, and the owner close-out. Approving with this list certifies every agent-doable item is 100% implemented. Never doable work — promote any owner-only claim a shell could satisfy to a blocker; an entry with missing or unverified substitute evidence belongs in blockers, not here. Absent when every item is agent-doable."

[schema.fields.remaining]
schema = "schema:text"
description = "Cross-task seam citations ONLY: counterpart work belonging to a DIFFERENT task on the task board, each citing that task's file name. Never in-task scope — an unimplemented item of THIS task's checklist is a blocker, and unimplemented in-task scope recorded here instead of in blockers falsifies the verdict. Absent when no cross-task seam exists."

[schema.fields.status]
schema = "schema:text"
required = true
description = "approved when no blocking defect remains (advisory notes may still exist); revise only when at least one blocking defect remains. Set to approved if and only if blockers is empty."
allowed = [
    "approved",
    "revise",
]

[schema.fields.wall-id]
schema = "schema:text"
required = true
description = 'Stable wall id copied VERBATIM from an explicit "**Wall:** <id>" label in the task file, non-empty only when status is revise and that label exists — never inferred from prose similarity or blocker content. Enables cross-run standing-wall refusal (P414); an empty string here never blocks a sibling run no matter how related its blockers look. Always present (never omitted) so the runtime can deterministically copy it into a park report without a missing-field failure.'

[[agent]]
id = "reviewer"
description = "Reviews an arbitrary git ref/range with no task board governing the run and writes both the typed verdict and the human review document."
summary = "Standalone review role."

[prompt.review]
text = """
Review the range {slot:range-resolved} (from the range/ref you were given: {port:range}). Commit log: {slot:commit-log}. File-level diff inventory (never the full patch): {slot:diff-stat}.
    Open the actual content yourself with your own tools — "git show", "git diff {slot:range-resolved} -- <file>" — the inventory above is only an index of where the changes landed.
    No task board governs this run: leave "wall-id" the empty string, omit "remaining" and "owner-items", and set "escalation" to "needs-owner" only for authority questions the diff itself raises, never for ordinary code-review blockers.
    Edit nothing — you are read-only. Open files, do not write them.
    Your own verdict from the previous round is attached as input when one exists — it is your review so far, and this round's verdict EXTENDS it rather than re-deriving it. For every carried blocker: keep its id and its steps verbatim (same order, same text); verify each open step's state directly with your tools and flip its status to done only on evidence you confirmed; append genuinely new findings as new steps at the end, or as new blockers; set recurrence-of on every carried blocker. DROP a blocker entirely once every step is done — name it in the advisory so the clearing is on record. The attached work summary is the worker's cumulative account and its claims of done are input to your verification, never a substitute for it.
                    Inspect the actual working tree with your tools — never review the summary alone; run the gates named in the phase's own Definition of Done if the summary does not prove they ran.
                    A BLOCKER makes the work genuinely unmergeable: a correctness bug, a house-rule violation, a gate the phase's own Definition of Done requires that is failing, clear over-build (accretion, defensive validation for impossible states, scope creep), OR un-abstracted duplication — logic re-implemented or copied where existing code should have been reused or a shared abstraction extracted. Everything else — subjective style, naming, taste, optional improvements, follow-up — is ADVISORY.
                    Report each blocker as a typed entry: a stable kebab-case id (reused verbatim if it returns), where (paths), what (the defect and its failure), root-cause (the missing invariant, not just where it shows), required-fix (the invariant an acceptable fix must establish and what to replace or delete — never just the cited call sites), and done-when (the falsifiable check you will apply next round). A blocker with recurrence-of set is proof the prior fix treated a symptom: state in root-cause why it was symptomatic, prescribe the structural fix in required-fix, and make clear that another point-patch at the cited sites will not clear it — for recurring duplication that means consolidate into one shared path and delete the copy, and do not approve until the duplicate is gone rather than the named case merely fixed. On a recurrence, two further duties. FIRST, enumerate the COMPLETE remaining divergence now — every responsibility the shared path must still absorb — not merely the symptom that exposed it this round: a worker who satisfies a partial list meets a longer list next round and learns that complying does not clear the blocker. SECOND, the required-fix must now name symbols: the exact functions/types to create, the exact functions that must no longer exist, and the call sites to route; and done-when must include at least one command the worker can run to see the structural state red or green for itself.
                    Put blocking defects in the blockers field and non-blocking notes in advisory. Set status to revise only when blockers is non-empty; otherwise approved — advisory never blocks. Do not promote taste to a blocker to earn another round. Return the typed verdict.
    Write both the typed verdict and a rendered human review document: what the range does, the blockers you found (if any) and why each is one, and anything advisory."""
input = [
    "port:range",
    "slot:range-resolved",
    "slot:commit-log",
    "slot:diff-stat",
]
output = [
    "slot:review-verdict",
    "slot:review-document",
]

[[sequence.ctx-branch-arm-p-14-range-resolved-then.sequence]]
id = "diff-stat"
title = "Capture the diff inventory"
input = ["slot:range-resolved"]
output = ["slot:diff-stat"]

[sequence.ctx-branch-arm-p-14-range-resolved-then.sequence.command]
argv = [
    "sh",
    "-c",
    'git diff --stat "$1"',
    "_",
    "{slot:range-resolved}",
]

[[sequence.ctx-branch-arm-p-14-range-resolved-then.sequence]]
id = "commit-log"
title = "Capture the commit log"
input = ["slot:range-resolved"]
output = ["slot:commit-log"]

[sequence.ctx-branch-arm-p-14-range-resolved-then.sequence.command]
argv = [
    "sh",
    "-c",
    'git log --oneline "$1"',
    "_",
    "{slot:range-resolved}",
]

[[sequence.ctx-branch-arm-p-14-range-resolved-then.sequence]]
id = "diff-non-empty"
title = "Diff Non-Empty"
kind = "branch"
sequence = "sequence:ctx-branch-arm-s-39-ctx-branch-arm-p-14-range-resolved-then-14-diff-non-empty-then"

[sequence.ctx-branch-arm-p-14-range-resolved-then.sequence.when.not]
slot = "slot:diff-stat"
equals = ""

[[sequence.ctx-branch-arm-s-39-ctx-branch-arm-p-14-range-resolved-then-14-diff-non-empty-then.sequence]]
id = "review"
title = "Review the range (reviewer)"
agent = "agent:reviewer"
prompt = "prompt:review"
input = [
    "port:range",
    "slot:range-resolved",
    "slot:commit-log",
    "slot:diff-stat",
]
output = [
    "slot:review-verdict",
    "slot:review-document",
]

[[sequence.ctx-branch-arm-s-39-ctx-branch-arm-p-14-range-resolved-then-14-diff-non-empty-then.sequence]]
id = "tree-status"
title = "Assert the reviewed tree stayed clean"
kind = "check"
output = ["slot:tree-status"]

[sequence.ctx-branch-arm-s-39-ctx-branch-arm-p-14-range-resolved-then-14-diff-non-empty-then.sequence.command]
argv = [
    "sh",
    "-c",
    'test -z "$(git status --porcelain)"',
]

[procedure]
description = "Preflight range resolution -> deterministic evidence capture (diff --stat, log --oneline) -> single reviewer pass -> read-only assertion."
input = ["port:range"]
output = [
    "port:review-document-report",
    "port:review-verdict-report",
    "port:tree-status-report",
]

[[procedure.sequence]]
id = "range-resolve"
title = "Resolve the range"
input = ["port:range"]
output = ["slot:range-resolved"]

[procedure.sequence.command]
argv = [
    "sh",
    "-c",
    'r="$1"; case "$r" in *...*|*..*) resolved="$r" ;; *) git rev-parse --verify "$r" >/dev/null 2>&1 || exit 0; base=$(git merge-base HEAD "$r" 2>/dev/null) || exit 0; resolved="$base..$r" ;; esac; git rev-list "$resolved" >/dev/null 2>&1 && echo "$resolved"; exit 0',
    "_",
    "{port:range}",
]

[[procedure.sequence]]
id = "range-resolved"
title = "Range Resolved"
kind = "branch"
sequence = "sequence:ctx-branch-arm-p-14-range-resolved-then"

[procedure.sequence.when.not]
slot = "slot:range-resolved"
equals = ""
