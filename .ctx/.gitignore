# P569: machine-local, never committed. Everything else under .ctx/traits/ —
# vendor.toml, vendor.lock, and the packages themselves — is the committed
# half and travels with the repo.
traits/runtime.toml
traits/vendored/
traits/generated/runtime.toml
worktrees/
config.ts
config.toml
harness.toml
runs/
debug/
cache/
node_modules/
traits/worktrees/
